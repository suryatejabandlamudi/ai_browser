diff --git a/chrome/browser/ui/browser.cc b/chrome/browser/ui/browser.cc
index 1234567..abcdefg 100644
--- a/chrome/browser/ui/browser.cc
+++ b/chrome/browser/ui/browser.cc
@@ -100,6 +100,7 @@
 #include "chrome/browser/ui/web_applications/web_app_launch_utils.h"
 #include "chrome/browser/ui/webui/chrome_web_ui_controller_factory.h"
 #include "chrome/browser/web_applications/web_app_provider.h"
+#include "chrome/browser/ai/ai_service.h"
 #include "chrome/common/buildflags.h"
 #include "chrome/common/chrome_constants.h"
 #include "chrome/common/chrome_features.h"
@@ -500,6 +501,12 @@ Browser::Browser(const CreateParams& params)
   if (search_model_)
     search_model_->AddObserver(this);
 
+  // Initialize AI service for local GPT-OSS 20B integration
+  ai_service_ = std::make_unique<ai::AIService>();
+  if (ai_service_) {
+    ai_service_->ConnectToLocalModel();
+  }
+
   if (profile_ && profile_->IsGuestSession()) {
     // Ensure guest profile is registered as off the record profile, and
     // also ensure it is registered before the call to TabStripModel::AddObserver
diff --git a/chrome/browser/ui/browser.h b/chrome/browser/ui/browser.h
index 2345678..bcdefgh 100644
--- a/chrome/browser/ui/browser.h
+++ b/chrome/browser/ui/browser.h
@@ -50,6 +50,10 @@ class LocationBarModelDelegate;
 class ScopedKeepAlive;
 class StatusBubble;
 
+namespace ai {
+class AIService;
+}
+
 namespace chrome {
 void NewEmptyWindow(Profile* profile);
 void NewWindowWithURL(const GURL& url, Profile* profile);
@@ -800,6 +804,9 @@ class Browser : public TabStripModelObserver,
   // Weak pointer. Owned by |profile_|.
   BookmarkModel* bookmark_model_;
 
+  // AI service for local processing
+  std::unique_ptr<ai::AIService> ai_service_;
+
   // The following factory is used to close the frame at a later time.
   base::WeakPtrFactory<Browser> weak_factory_;
 